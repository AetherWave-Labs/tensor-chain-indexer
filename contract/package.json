{
  "name": "@tensor-chain-indexer/contract",
  "version": "0.1.0",
  "private": true,
  "description": "Smart contract development and testing workspace for Tensor Chain Indexer",
  "type": "module",
  "scripts": {
    "build": "hardhat compile",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^3.0.0",
    "typescript": "^5.7.0"
  }
}